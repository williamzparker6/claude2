{
  "name": "rms-meridian",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RMS Meridian — a narrative puzzle-platformer vertical slice (Three.js).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
